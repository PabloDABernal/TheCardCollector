{
  "name": "@collector/combat-scene",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "scripts": {
    "predev": "node scripts/sync-data.mjs",
    "dev": "vite",
    "prebuild": "node scripts/sync-data.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "verify:visual": "playwright test e2e/"
  },
  "dependencies": {
    "@collector/combat-bridge": "*",
    "@collector/domain-catalog": "*",
    "@collector/domain-combat": "*",
    "@collector/domain-shared": "*",
    "phaser": "^3.80.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "vite": "^5.0.0"
  }
}
